# generated by dockerfile-rails

---
options:
  migrate: bin/deploy
  passenger: true
  platform: linux/amd64
  procfile: Procfile.fly
  variant: slim-bullseye
  packages:
    deploy:
    - sqlite3
    - redis-server
    - procps
    - vim
    - rsync
    - openssh-server
    - dnsutils
  envs:
    deploy:
      RAILS_STORAGE: "/data/storage"
      RAILS_DB_VOLUME: "/data/db"
  instructions:
    deploy: config/deploy.fly

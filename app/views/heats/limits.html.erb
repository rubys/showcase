<div class="mx-auto w-full">
  <% if notice.present? %>
    <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-lg inline-block" id="notice"><%= notice %></p>
  <% end %>

  <div class="flex justify-between items-center mb-8">
    <h1 class="font-bold text-4xl">Heat Limits</h1>
    <div>
      <%= link_to 'Back to Heats', heats_path, class: "btn-blue" %>
    </div>
  </div>

  <div class="mb-8">
    <h2 class="text-2xl font-bold mb-4">Overall Configuration</h2>
    <p class="text-gray-600 mb-4">
      Default heat limit per person/dance/category: <strong><%= @overall_limit %></strong> heats
    </p>
    <p class="text-sm text-gray-500">
      Some dances have custom limits that override the default.
    </p>
  </div>

  <% if @violations.any? %>
    <div class="mb-8 p-4 bg-red-50 border border-red-200 rounded-lg">
      <h2 class="text-2xl font-bold text-red-600 mb-4">
        <span class="text-red-500">⚠️</span> Violations Found
      </h2>
      <p class="text-red-700 mb-4">
        <strong><%= pluralize(@violations.count, 'violation') %></strong> found across
        <strong><%= @violations.map { |v| v[:person] }.uniq.count %></strong> people.
      </p>

      <div class="overflow-x-auto">
        <table class="min-w-full bg-white border border-gray-200 rounded-lg">
          <thead class="bg-gray-50">
            <tr>
              <th class="px-4 py-2 text-left text-sm font-medium text-gray-700 border-b">Person</th>
              <th class="px-4 py-2 text-left text-sm font-medium text-gray-700 border-b">Dance</th>
              <th class="px-4 py-2 text-left text-sm font-medium text-gray-700 border-b">Category</th>
              <th class="px-4 py-2 text-center text-sm font-medium text-gray-700 border-b">Total Heats</th>
              <th class="px-4 py-2 text-center text-sm font-medium text-gray-700 border-b">Lead / Follow</th>
              <th class="px-4 py-2 text-center text-sm font-medium text-gray-700 border-b">Limit</th>
              <th class="px-4 py-2 text-center text-sm font-medium text-gray-700 border-b">Excess</th>
            </tr>
          </thead>
          <tbody>
            <% @violations.each_with_index do |violation, index| %>
              <tr class="<%= index.even? ? 'bg-white' : 'bg-gray-50' %> hover:bg-red-50">
                <td class="px-4 py-2 text-sm border-b">
                  <%= link_to violation[:person], person_path(violation[:person_id]),
                      class: "text-blue-600 hover:text-blue-800 underline" %>
                </td>
                <td class="px-4 py-2 text-sm border-b">
                  <%= link_to violation[:dance], heats_dance_path(violation[:dance_id]),
                      class: "text-blue-600 hover:text-blue-800 underline" %>
                  <% if violation[:is_custom_limit] %>
                    <span class="inline-block ml-1 px-2 py-1 text-xs bg-orange-100 text-orange-700 rounded">
                      Custom Limit
                    </span>
                  <% end %>
                </td>
                <td class="px-4 py-2 text-sm border-b"><%= violation[:category] %></td>
                <td class="px-4 py-2 text-sm text-center border-b font-medium text-red-600">
                  <%= violation[:count] %>
                </td>
                <td class="px-4 py-2 text-sm text-center border-b">
                  <span class="text-blue-600"><%= violation[:lead_count] %></span>
                  /
                  <span class="text-purple-600"><%= violation[:follow_count] %></span>
                </td>
                <td class="px-4 py-2 text-sm text-center border-b"><%= violation[:limit] %></td>
                <td class="px-4 py-2 text-sm text-center border-b">
                  <span class="px-2 py-1 bg-red-100 text-red-700 rounded font-medium">
                    +<%= violation[:excess] %>
                  </span>
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
  <% else %>
    <div class="mb-8 p-4 bg-green-50 border border-green-200 rounded-lg">
      <h2 class="text-2xl font-bold text-green-600 mb-2">
        <span class="text-green-500">✅</span> No Violations
      </h2>
      <p class="text-green-700">
        All people are within their dance-specific heat limits.
      </p>
    </div>
  <% end %>

  <div class="mb-8">
    <h2 class="text-2xl font-bold mb-4">Dance Limits Reference</h2>
    <p class="text-gray-600 mb-4">
      Current limit configuration for each dance:
    </p>

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
      <% @dance_limits.each do |dance_limit| %>
        <div class="p-4 bg-white border border-gray-200 rounded-lg">
          <div class="flex justify-between items-start mb-2">
            <h3 class="font-medium text-gray-900">
              <%= link_to dance_limit[:name], heats_dance_path(dance_limit[:id]),
                  class: "text-blue-600 hover:text-blue-800 underline" %>
            </h3>
            <% if dance_limit[:is_custom] %>
              <span class="inline-block px-2 py-1 text-xs bg-orange-100 text-orange-700 rounded">
                Custom
              </span>
            <% else %>
              <span class="inline-block px-2 py-1 text-xs bg-gray-100 text-gray-600 rounded">
                Default
              </span>
            <% end %>
          </div>
          <div class="text-2xl font-bold <%= dance_limit[:is_custom] ? 'text-orange-600' : 'text-gray-600' %>">
            <%= dance_limit[:effective_limit] %>
            <span class="text-sm font-normal text-gray-500">heats</span>
          </div>
        </div>
      <% end %>
    </div>
  </div>

  <div class="mt-8 p-4 bg-blue-50 border border-blue-200 rounded-lg">
    <h3 class="text-lg font-medium text-blue-800 mb-2">How Heat Limits Work</h3>
    <ul class="text-sm text-blue-700 space-y-1">
      <li>• Each person has a limit per dance per category (e.g., Closed/Waltz, Open/Cha Cha)</li>
      <li>• Limits count total heats on the floor (Lead + Follow combined)</li>
      <li>• Some dances have custom limits that override the default of <%= @overall_limit %> heats</li>
      <li>• Violations occur when a person's total heats exceed their limit for any dance/category combination</li>
    </ul>
  </div>
</div>
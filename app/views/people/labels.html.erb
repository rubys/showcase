<!DOCTYPE html>
<html>
  <head>
    <title>Showcase</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= action_cable_meta_tag_dynamic %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <style>
    @page {
      size: 6in 4in; /* letter landscape */
      margin: 0.2in 0in;
      padding: 0;
    }
    </style>
  </head>

  <body>
    
    <% @people.each_with_index do |person, index| %>
    <% if index % 1 == 0 # % 4 %>
    <% if index > 0 %>
    </div>
    <% end %>
    <div class="break-inside-avoid grid grid-cols-2" style="grid-row-gap: 0.9in">
    <% end %>
    <div class="grid grid-cols-1 text-center" style="min-width: 6in; max-width: 6in; min-height: 3in; max-height: 3in">
    <%= image_tag showcase_logo, class: "mx-auto w-28" %>
    <div>
    <div class="text-xl"><%= @event.name %></div>
    <div><%= @event.date %></div>
    </div>
    <div class="my-6">
    <div class="text-5xl mb-4"><%= person.display_name %></div>
    <div class="text-2xl"><%= person.studio&.name%></div>
    <% if person.studio_id == 0 %>
    <div class="text-xl"><%= person.type %></div>
    <% else %>
    <div class="text-4xl"><%= person.back %></div>
    <% end %>
    </div>
    </div>
    <% end %>
    </div>

  </body>
</html>

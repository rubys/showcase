<div>

<p class="mb-4">Websocket: <%= @websocket %></p>

<table>
<tbody>
  <% request.env.sort.each do |key, value| %>
    <tr>
      <td><%= key %>
      <td><%= value %>
    </tr>
  <% end %>
</tbody>
</table>

</div>
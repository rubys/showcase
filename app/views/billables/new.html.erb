<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">New <%= @type %></h1>

  <%= render "form", billable: @billable %>


<div class="mx-auto grid grid-cols-2">
  <input type="submit" class="btn-blue" form="billable-form" value="Create <%= @type %>">

  <%= link_to 'Back to Billables', billables_path, class: "btn-grey" %>
</div>
</div>

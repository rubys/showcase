<div class="w-full">
  <% if notice.present? %>
    <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-lg inline-block" id="notice"><%= notice %></p>
  <% end %>

  <h1 class="mt-16 font-bold text-4xl mb-6">Billable Items</h1>

  <%= render partial: 'index', locals: { id: 'billables' } %>

  <input type="submit" name="commit" value="Update prices" class="btn-blue" form="billable-form" data-disable-with="Update prices">

  </div>
</div>

# generated by dockerfile-rails

---
options:
  migrate: bin/deploy
  passenger: true
  platform: linux/amd64
  procfile: Procfile.fly
  sentry: true
  swap: 2G
  packages:
    deploy:
    - sqlite3
    - redis-server
    - procps
    - vim
    - rsync
    - openssh-server
    - dnsutils
    - sudo
    - poppler-utils
  envs:
    deploy:
      RAILS_STORAGE: "/data/storage"
      RAILS_DB_VOLUME: "/data/db"
      RAILS_LOG_VOLUME: "/data/log"
  instructions:
    build: config/build.fly
    deploy: config/deploy.fly
